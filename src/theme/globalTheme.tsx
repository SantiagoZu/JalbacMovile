import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    globalMargin: {
        marginHorizontal: 20,
    },
    title: {
        fontSize: 30,
        color: '#dfe5ea',
        fontWeight: "bold",
    },
    parrafo: {
        fontSize: 20,
        color: '#86898e',
        fontWeight: "600",
        marginTop: 10
    },
});