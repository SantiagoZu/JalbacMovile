import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    globalMargin: {
        marginHorizontal: 15,
    },
    title: {
        fontSize: 30,
        color: '#dfe5ea',
        fontWeight: "bold",
    },
    parrafo: {
        fontSize: 20,
        color: '#86898e',
        fontWeight: "600",
        marginTop: 10
    },
});

export const colors = {
    primaryColor: '#121317',
    secondaryColor: '#1a1c23',
    colorIcon: '#CABFFD'
}